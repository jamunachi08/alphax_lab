[project]
name = "alphax_lab"
authors = [{ name = "Neotec Integrated Solutions", email = "support@neotec.sa" }]
description = "Lab consumable consumption for Plasma-integrated ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "alphax_lab"
